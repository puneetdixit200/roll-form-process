export type VisualVertex = { vertex_id: string; x: number; y: number };
export type VisualSegment = { segment_id: string; type: "LINE" | "ARC"; start_vertex_id: string; end_vertex_id: string; center?: { x: number; y: number }; radius?: number; clockwise?: boolean };
export type VisualProfile = { schema_version: 1; profile_id: string; name: string; topology: "OPEN_PATH" | "CLOSED_CONTOUR"; closed: boolean; computational_seam_vertex_id: string | null; vertices: VisualVertex[]; segments: VisualSegment[]; metadata: { source: string; visual_only: true } };
export type VisualCandidate = { candidate_id: string; candidate_style: string; station_count: number; status: string; visual_confidence: { score: number; band: string; mean_pass_confidence: number; minimum_pass_confidence: number; progression_smoothness: number; non_calibrated: boolean }; passes: Array<{ pass_id: string; order: number; progress: number; profile: { points: number[][]; topology: string }; historical_match: { best_match: any; top_matches: any[] }; visual_confidence: { score: number; band: string }; warnings: string[] }>; warnings: string[] };
export type VisualRun = { run_id: string; status: string; candidates: VisualCandidate[]; warnings: string[] };
